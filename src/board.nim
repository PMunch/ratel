import private/helpers

const board* {.strdefine.} = ""

static:
  echo "----------------------------------"
  echo "The current board: ", board
  echo "----------------------------------"

generateInclude()
